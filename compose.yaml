version: '3'

services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=postgre_db_mdy0'
      - 'POSTGRES_PASSWORD=UTHkndH07S5ZUqcc9LtDpPvO6JzZgBRB'
      - 'POSTGRES_USER=albatros'
    ports:
      - '5432'

  cache:
    image: "redis"
    ports:
      - "6379:6379"
    environment:
      - ALLOW_EMPTY_PASSWORD=yes

  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - cache
      - postgres



